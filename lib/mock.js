function versions(resolvers, module) {
  var _versions = []
  var resolver  = {
    type   : 'aws',
    key    : '1234',
    secret : '43121'
  }
  return [_versions, resolver]
}

function resolvers() {
  return [
    {
      type: 'aws',
      bucket:
    }
  ]
}

// makes a stream from a resolver, module, and version
function stream(resolver, module, version) {

}

function wanker() {
  
}